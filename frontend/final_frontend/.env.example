# Leave empty for local Vite development because /api is proxied to Spring Boot.
# For a deployed frontend, set this to the backend origin, for example:
# VITE_API_URL=https://your-backend.example.com/api
VITE_API_URL=
